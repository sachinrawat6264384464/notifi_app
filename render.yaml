services:
  - type: web
    name: botmartz-ai-backend
    env: python
    rootDir: backend
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: APP_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        value: postgresql://neondb_owner:npg_f5HqoLObEca2@ep-aged-pine-a5k7hfx1-pooler.us-east-2.aws.neon.tech/neondb?sslmode=require
      - key: CORS_ORIGINS
        value: '["*"]'
